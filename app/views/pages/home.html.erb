<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>РЗТ</title>
  <style>
      body {
          font-family: 'Arial', sans-serif;
          margin: 0;
          padding: 0;
          background-color: #f4f4f4;
      }

      .main-container {
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          height: 100vh;
      }

      header {
          text-align: center;
          margin-bottom: 20px;

          & img {
              max-width: 100px;
              height: auto;
          }

          & h1 {
              font-size: 2em;
              margin-top: 10px;
              color: #333;
          }
      }

      .content-container {
          display: flex;
          gap: 20px;

          .content-btn {
              flex-grow: 1;
              padding: 10px;
              background-color: #4285f4;
              color: #fff;
              text-decoration: none;
              text-align: center;
              border-radius: 5px;
              transition: background-color 0.3s ease-in-out;
          }

          .content-btn:hover {
              background-color: #3367d6;
          }
      }

      /* Медиа-запрос для адаптивности */
      @media (max-width: 600px) {
          .content-container {
              flex-direction: column;
          }
      }
  </style>
</head>
<body>

<div class="main-container">
  <header>
    <%= image_tag "logo.png", alt: "Logo" %>
    <h1>РЗТ</h1>
  </header>

  <div class="content-container">
    <% if current_user %>
      <a href="<%= edit_user_path(current_user) %>" class="btn btn-lg content-btn">Мой профиль</a>
      <a href="<%= logout_path %>" data-confirm="Вы уверены?" data-method="delete" class="btn btn-lg content-btn">Выйти из аккаунта</a>
    <% else %>
      <a href="<%= signup_path %>" class="btn btn-lg content-btn">Зарегистрируйтесь сейчас!</a>
      <a href="#" class="btn btn-lg content-btn" data-toggle="modal" data-target="#loginModal">Войдите</a>
    <% end %>

    <a href="<%= content_path %>" class="btn btn-lg content-btn">Смотреть контент</a>
  </div>
</div>
</body>
</html>
